name: Link Check

on:
  pull_request:
    paths:
      - "**/*.md"
      - "site/**/*.html"
      - ".lycheeignore"
      - "lychee.toml"
      - ".github/lychee-ci.toml"
      - "mise.toml"
      - "mise.lock"
      - ".github/workflows/link-check.yml"
  schedule:
    - cron: "17 4 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lychee:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: jdx/mise-action@5228313ee0372e111a38da051671ca30fc5a96db # v3
        with:
          version: 2026.9.5
          install_args: --locked lychee

      # Every link, but only gone links and missing anchors fail here; see
      # .github/lychee-ci.toml. The pre-push hook checks strictly.
      - name: Check Markdown and site links
        run: >-
          lychee --config lychee.toml --config .github/lychee-ci.toml
          './**/*.md' './site/**/*.html'
