{
  "evals": [
    {
      "name": "choose-visual-layer",
      "prompt": "This React app has Vitest unit tests and Playwright E2E tests, but CSS refactors keep breaking production. Propose what tests to add.",
      "expected": "Recommends Storybook/component state coverage plus visual regression instead of more brittle DOM assertions."
    },
    {
      "name": "harden-codegen",
      "prompt": "Use Playwright codegen output to create a maintainable checkout smoke test.",
      "expected": "Treats generated code as a draft, improves locators, removes incidental waits, scopes assertions to the user goal, and keeps the flow small."
    },
    {
      "name": "stabilize-visual-test",
      "prompt": "A visual regression test flakes across macOS laptops and Linux CI.",
      "expected": "Diagnoses environment and dynamic rendering differences, recommends stable browser/container/font setup, masking, disabled animations, and intentional thresholds."
    },
    {
      "name": "rsc-boundary",
      "prompt": "A Next.js page fetches product data and renders an interactive filter sidebar. Decide server/client boundaries.",
      "expected": "Keeps data access and static product rendering server-side, isolates filter interactivity in client components, and explains serialization and bundle tradeoffs."
    },
    {
      "name": "rerender-diagnosis",
      "prompt": "A React dashboard lags while typing into a search box. The team wants to add useMemo everywhere.",
      "expected": "Diagnoses state ownership and render paths first, then applies targeted deferral, memoization, or component boundary changes."
    },
    {
      "name": "choose-state-ownership",
      "prompt": "Design a reusable React Combobox component that can be controlled by a form library but also work standalone.",
      "expected": "Defines controlled/uncontrolled boundaries, accessible DOM requirements, refs, and state callbacks without replacing native semantics blindly."
    },
    {
      "name": "polymorphic-api-review",
      "prompt": "Review this React Button component that supports an as prop for links, buttons, and divs.",
      "expected": "Challenges unsafe polymorphism, preserves semantic button/link behavior, and requires accessible names, focus, disabled, and event behavior."
    },
    {
      "name": "typography-system-review",
      "prompt": "Review a product UI that uses one-off text sizes, font-variation-settings for weight, synthetic italic, and custom link underlines that cut through descenders.",
      "expected": "Introduces a small role-based type system, prefers high-level CSS font properties, keeps root font-synthesis none while requiring real loaded faces and resilient fallbacks, and fixes underlines with font metrics and skip-ink."
    },
    {
      "name": "polish-nested-controls",
      "prompt": "Polish a compact card with equal parent/child border radii, a visibly off-center arrow icon, and two 20px icon buttons whose expanded hit areas overlap.",
      "expected": "Applies concentric radii only where the nested geometry warrants it, corrects the source icon or makes a documented optical adjustment, and provides large non-overlapping targets without imposing universal animation or shadow magic numbers."
    },
    {
      "name": "migrate-color-representation",
      "prompt": "Convert an existing tokenized theme from hex and rgb to OKLCH without redesigning it, including gradients, alpha colors, third-party chart configuration, light/dark states, and contrast checks.",
      "expected": "Preserves token semantics and CSS structure, leaves formats required by third parties alone, uses tested conversion and gamut handling, meets applicable WCAG 2.2 AA contrast requirements, and uses APCA as a supplementary perceptual check."
    },
    {
      "name": "defer-expensive-subtree",
      "prompt": "A controlled search input still lags after the developer passes its query through useDeferredValue, but the expensive results component is not memoized and receives a new options object on every render. Diagnose and fix the design.",
      "expected": "Explains that deferral needs a skippable memoized boundary for CPU-heavy child rendering, stabilizes the props consumed by that boundary, keeps the input synchronous, and distinguishes rendering prioritization from request debouncing."
    },
    {
      "name": "evaluate-progressive-enhancement",
      "prompt": "A healthcare appointment flow wants to adopt a CSS feature with 94% global support. @supports returns true in the team's main browser, but deleting the feature makes the booking controls overlap. Decide whether and how to ship it.",
      "expected": "Evaluates the actual fallback, product browser distribution, partial-implementation risk, and high harm of a blocked essential task; requires a usable fallback and representative-browser testing rather than approving it from the percentage or @supports result alone."
    },
    {
      "name": "design-earned-motion",
      "prompt": "Add a playful arrow micro-interaction to a branded onboarding completion link without making the workflow slower or excluding keyboard and reduced-motion users.",
      "expected": "Allows restrained earned delight, prefers a brief event-driven impulse when persistence is unnecessary, supports focus or activation as well as hover, preserves native link semantics, and keeps the reduced-motion path static and complete."
    },
    {
      "name": "choose-animation-engine",
      "prompt": "The team wants a JavaScript animation library solely to express basic hover fades, but also needs one gesture-driven SVG morph elsewhere. Choose the implementation strategy and explain the performance checks.",
      "expected": "Uses native CSS for basic state transitions, considers WAAPI or a capable library only for the morph and gesture requirement, distinguishes browser-engine animation from main-thread per-frame loops, and measures the actual properties and rendering cost."
    },
    {
      "name": "build-accessible-svg",
      "prompt": "Turn an exported SVG illustration into a responsive themed React component with a small decorative path animation. The export has fixed dimensions, no viewBox, generated IDs, and an interactive click handler on the svg element itself.",
      "expected": "Repairs the real artwork bounds and viewBox, chooses inline delivery for theming, moves interaction to a native button or link, defines decorative or informative naming explicitly, makes motion keyboard-equivalent and reduced-motion-safe, and audits generated IDs and optimization."
    },
    {
      "name": "diagnose-percentage-height",
      "prompt": "A child with height: 100% stays content-height inside a parent whose height is auto. The proposed fix sets 100% height on every ancestor. Diagnose the sizing chain and choose a resilient layout.",
      "expected": "Explains the circular percentage-height dependency and definite-size requirement, then prefers min-height, flex or grid stretch, intrinsic sizing, or an intentionally definite ancestor instead of blindly propagating 100% heights."
    },
    {
      "name": "design-elevation-shadows",
      "prompt": "Create raised and overlay shadow tokens for a branded interface on a tinted background. The current proposal uses one fuzzy transparent-black shadow everywhere and animates it on every card hover.",
      "expected": "Defines a consistent light direction, tinted semantic shadow colors, sharper and more opaque low elevation plus softer and less opaque high elevation, limits layering to places that earn it, and avoids animating repeated layered shadows without performance evidence."
    },
    {
      "name": "choose-ai-interface-modality",
      "prompt": "Add AI to a warehouse inspection workflow. The team proposes a chatbot, but technicians wear gloves, cannot safely read long responses while moving, and must verify every safety-relevant recommendation.",
      "expected": "Audits input, output, social, and cognitive constraints; rejects chat as an automatic default; proposes an appropriate hands-free or glanceable capture path plus a persistent review-and-approve surface; exposes evidence and uncertainty; and preserves manual, cancel, and recovery paths."
    },
    {
      "name": "operationalize-accessibility",
      "prompt": "A team runs one automated accessibility scan before each release and keeps finding the same keyboard and focus regressions late. Redesign the quality process without treating a green scan as proof.",
      "expected": "Moves accessibility requirements into component contracts and acceptance criteria, assigns severity and ownership, adds static/component/CI checks at the cheapest layers, retains manual keyboard and assistive-technology review for consequential flows, and fixes recurring defects in shared primitives or process."
    },
    {
      "name": "audit-agent-generated-ui",
      "prompt": "An inherited agent-generated app looks fine on its main route, but has hundreds of component files, sparse stories, conflicting tokens, and an AGENTS.md that may be stale. Plan a Storybook-based audit.",
      "expected": "Builds a deterministic live/dead/unknown inventory before trusting prose or deleting files, renders every meaningful conditional state, surfaces token and primitive drift, combines automated accessibility checks with manual verification, maps routes and journeys, and stores severity-aware findings that can be diffed over time."
    },
    {
      "name": "promote-recent-baseline-feature",
      "prompt": "A form uses JavaScript to measure and auto-grow a textarea because the model says CSS cannot do it reliably. The project targets Baseline 2026 but also supports a few older enterprise browsers. Review the implementation.",
      "expected": "Surfaces Baseline 2026 Newly available field-sizing: content despite an older model cutoff, proposes it as the primary current-platform implementation, bounds and tests its sizing behavior, preserves a coherent fixed-size fallback for older browsers, and verifies the exact current feature status rather than relying on memory."
    },
    {
      "name": "enhance-customizable-select",
      "prompt": "Replace a custom color picker combobox with customizable select. The design removes all option text in favor of swatches and assumes the new select styling is Baseline because select itself is widely available.",
      "expected": "Separates the subfeature's Limited availability from the select element's status, keeps real option text and native semantics, makes swatches additive, support-gates the visual enhancement, and tests the classic fallback, accessible value, mobile picker, hydration, and form submission."
    },
    {
      "name": "optimize-lazy-responsive-images",
      "prompt": "A card grid has complex container-driven widths. Every below-the-fold image is lazy-loaded and has a srcset, but the team maintains brittle hand-calculated sizes strings. The hero image is also lazy-loaded. Modernize the markup without regressing older browsers or LCP.",
      "expected": "Uses sizes=\"auto, <descriptive fallback>\" with explicit dimensions for the lazy-loaded card images, preserves a fallback for unsupported browsers, and removes lazy loading from the hero/LCP image while giving it an accurate descriptive sizes value and early discovery."
    },
    {
      "name": "stabilize-streaming-output",
      "prompt": "An AI transcript app appends every token directly to the DOM, always snaps to the bottom, leaves buffered text after Stop, and announces every token. Redesign the streaming behavior.",
      "expected": "Batches DOM writes to animation frames, follows only while the reader remains near the end, preserves manual scrolling, fully clears and marks canceled partial output, keeps retry context and keyboard controls, and uses a tested polite transcript announcement strategy without token-level speech."
    },
    {
      "name": "build-clickable-card-controls",
      "prompt": "Make an entire article card clickable while retaining its bookmark button and overflow menu. The proposed markup nests both buttons inside the article link.",
      "expected": "Rejects nested interactive content, keeps the primary link and secondary buttons as semantic siblings, optionally expands the primary link with a positioned pseudo-element below raised secondary controls, and verifies hit-area separation, focus order, visible focus, selection, and accessible names."
    },
    {
      "name": "preserve-tonal-gradient-chroma",
      "prompt": "A theme generator creates the darker stop of every brand gradient by reducing HSL lightness by 20%. A designer proposes always rotating the darker stop's hue by 14 degrees instead because one blue example looks more vibrant. Define a general approach that can work across arbitrary brand colours.",
      "expected": "Converts to OKLCH, chooses target lightness, keeps hue fixed initially, preserves and gamut-maps as much chroma as the target gamut permits, and treats any hue trajectory as curated palette-specific art direction rather than a universal offset. It also chooses gradient interpolation deliberately and verifies foreground contrast across the full gradient."
    },
    {
      "name": "layer-wide-gamut-token-fallbacks",
      "prompt": "A design system replaces its brand accent with one high-chroma Display P3 value in every component. The author wraps it in @supports (color: color(display-p3 0 0 0)) and assumes that covers older browsers, sRGB displays, gradients, and text contrast. Review and correct the architecture.",
      "expected": "Separates colour-syntax support from output-device gamut, defines a controlled sRGB semantic-token fallback first, gates the P3 token with both the relevant @supports query and @media (color-gamut: p3), and keeps gamut variants at the token-owning scope. It derives and gamut-maps each variant for its target gamut and verifies foreground/background pairs, gradients, states, and themes in both branches rather than relying on automatic gamut mapping."
    },
    {
      "name": "measure-geo-without-false-precision",
      "prompt": "A GEO vendor ran 20 prompts once, reports a 38% AI share of voice, and claims the campaign generated revenue. Search Console shows more Web impressions, but analytics has no reliable AI referral or conversion view. Design a defensible measurement plan.",
      "expected": "Treats the vendor result as a dated sample, builds a versioned query panel across relevant engines, locales, and repeated runs, retains raw answers and cited URLs, and separates mentions and citations from factual accuracy, referrals, engagement, and business outcomes. It notes that Google AI feature data is blended into Search Console Web reporting and rejects causal ROI claims without supporting acquisition and conversion evidence."
    },
    {
      "name": "refresh-citable-editorial-content",
      "prompt": "A publisher wants to improve GEO by changing every article's dateModified and sitemap lastmod today, adding an unlinked 'Editorial Team' byline, and appending generic FAQs without reviewing the old claims. Review the plan and propose a better maintenance workflow.",
      "expected": "Rejects fake freshness and generic authority signals, prioritizes claim-level review and correction, assigns owners and risk-based review cadences, adds truthful visible bylines linked to relevant author or reviewer profiles, aligns material publication and update dates with structured data and sitemap signals, and strengthens answer usefulness with real evidence, methods, limitations, and direct responses rather than filler FAQs."
    },
    {
      "name": "route-ambiguous-product-direction",
      "prompt": "Build a new customer-management dashboard. The repository has no comparable screen or design ADR, and the prompt does not say whether daily support operators or occasional small-business owners are the primary users.",
      "expected": "Recognizes that the audience changes density, hierarchy, guidance, tone, and interaction strategy; asks one concise discriminating question rather than a broad style interview; then states a Design Read with product register, direction axes, communication relationship, signature opportunity, and category default to avoid before implementation."
    },
    {
      "name": "combine-premium-minimalist-brand-direction",
      "prompt": "Design a premium minimalist landing page for an evidence-led financial service. The team assumes premium requires glass pills, cream backgrounds, serif headings, giant whitespace, and animated scroll sections.",
      "expected": "Treats premium execution and minimalist restraint as compatible rather than fixed presets, keeps trust and evidence primary, selects one justified signature, uses scarce motion and materiality, rejects category-reflex styling, records durable direction only if it is a project-level choice, and preserves accessibility, performance, and credible real content."
    },
    {
      "name": "honor-design-and-voice-adrs",
      "prompt": "An accepted ADR says the daily operations console is dense, precise, low-motion, and uses professional German du-address. A redesign request proposes sparse cinematic screens, playful error jokes, and Sie copy without mentioning the ADR. Review the request.",
      "expected": "Reads the ADR as the current cross-channel constraint, identifies visual and language conflicts, keeps sober task and error behavior, and either restores alignment or asks for authorization to supersede the decision with changed drivers, consequences, channel exceptions, and review triggers rather than silently drifting or rewriting the old ADR."
    },
    {
      "name": "decouple-compound-component-state",
      "prompt": "A reusable checkout step has showSummary, showCoupon, useUrlState, useFormState, compact, and isConfirmation booleans. Its child controls import one form library directly, so the same UI cannot be used in a standalone preview. Redesign the component API.",
      "expected": "Separates genuine binary semantics from mutually exclusive modes, creates explicit structural variants or compound components, defines a small state-actions-meta provider contract, keeps the provider as the owner of form, URL, or local implementation details, scopes context by update frequency, and preserves semantic and accessible primitives."
    },
    {
      "name": "prioritize-react-performance-work",
      "prompt": "A Next.js dashboard has serial server fetches, a heavy editor in the initial bundle, mutable module-level currentTenant state, unversioned localStorage preferences, and a cheap array map. The team wants to optimize the map first. Prioritize and correct the work.",
      "expected": "Prioritizes the network waterfall, bundle boundary, and cross-request tenant leak before client persistence and measured render or loop work; starts independent promises early, isolates the heavy editor behind an appropriate dynamic boundary, removes request data from module state, defines safe cache and authorization boundaries, versions and validates minimal storage, and rejects unmeasured micro-optimization as the first step."
    },
    {
      "name": "preserve-redesign-contracts",
      "prompt": "Modernize an established subscription site without changing its business logic. The visual proposal also renames navigation labels, changes route slugs, rewrites German du copy to Sie, replaces form field names, and removes analytics attributes and the existing consent component.",
      "expected": "Classifies the work as a preservation redesign, captures the relevant baseline, treats routes, IA, communication, form, analytics, consent, and accessibility behavior as protected contracts, limits visual modernization to the authorized scope, and routes any durable intentional contract change through explicit approval and the appropriate ADR or delivery artifact."
    },
    {
      "name": "bound-visual-overhaul-scope",
      "prompt": "The client approved a complete visual overhaul of a content site. The designer assumes this permits replacing the CMS structure, rewriting all articles, changing canonical URLs, and migrating from Next.js to another framework.",
      "expected": "Treats visual overhaul as a new visual language rather than blanket product and content authority, preserves content, IA, canonical and route contracts, and the existing stack unless separately justified and authorized, names the migration risks, and verifies redesigned output against the baseline."
    },
    {
      "name": "honor-design-system-provenance",
      "prompt": "A Shopify admin app already uses the official platform component system. A redesign imports components from two unrelated design systems and hand-copies the appearance of missing Shopify patterns because the designer prefers their defaults.",
      "expected": "Keeps one owning official system, verifies its current packages and supported patterns, avoids mixing complete systems or approximating official behavior by appearance, bridges genuine gaps explicitly within the existing stack, and records a durable system replacement only through an authorized decision and migration plan."
    },
    {
      "name": "classify-generated-ui-cluster",
      "prompt": "Review a new B2B automation landing page with no design system or ADR. It uses a cream background, an italic serif hero with an uppercase eyebrow pill, six identical rounded feature cards with icon tiles, a glowing gradient CTA, and the line 'Not just automation. A revolution.' The designer argues that every individual choice is common and accessible.",
      "expected": "Does not mislabel stylistic familiarity as an accessibility defect; classifies the individual tells as advisories, recognizes their cross-domain cluster as evidence of an unexamined category template, and revises the owning direction, evidence, hierarchy, media, and copy choices instead of swapping decorations one by one."
    },
    {
      "name": "preserve-justified-antipattern-exceptions",
      "prompt": "An editorial publication's accepted design system uses one type family, an italic serif feature headline, and 01/02/03 markers for a real three-step investigative method. A generic UI scanner flags all three as AI slop. Review the findings.",
      "expected": "Treats the documented editorial register, coherent type system, and meaningful sequence as accepted exceptions rather than style violations; still verifies legibility, hierarchy, responsiveness, and implementation quality; and rejects font blacklists or context-free suppression of intentional brand language."
    },
    {
      "name": "separate-antipattern-evidence-from-judgment",
      "prompt": "Audit an inherited admin screen that uses Inter and a decorative grid background, but also contains empty image sources, low-contrast labels, a menu clipped by overflow hidden, and a mobile layout that removes approval actions. Prioritize the findings.",
      "expected": "Prioritizes the verified broken images, contrast, clipped interaction, and lost mobile capability by user impact; treats the font and decorative grid as contextual advisories; checks for a broader generic-output cluster; and does not present a clean or noisy automated scan as proof of design quality."
    },
    {
      "name": "rewrite-generated-copy-without-voice-drift",
      "prompt": "A German marketing page has an accepted professional du voice. Every section uses a variation of 'Nicht nur X. Sondern Y.', generic transformation claims, and repeated em dashes. Rewrite it without switching to Sie or banning punctuation globally.",
      "expected": "Preserves the recorded professional du relationship, treats repeated cadence, unsupported claims, and punctuation density as contextual signals rather than forbidden tokens, rewrites with concrete product nouns, verbs, evidence, and limits, and routes any durable voice change through an ADR instead of silently changing formality."
    },
    {
      "name": "reconcile-design-artifact-with-browser-evidence",
      "prompt": "A signed-off Figma design shows three offset sticky cards at a tall desktop size. In a browser prototype, the final card becomes unreachable on short laptop viewports and keyboard focus can remain behind another card. The developer says the website is the source of truth and wants to simplify it without involving design.",
      "expected": "Treats the accepted design intent as binding but the static geometry as an incomplete responsive hypothesis, documents the short-viewport and focus evidence, proposes a simpler usable core and optional enhancement, shows it in a resizable example, and seeks approval for the intentional divergence instead of silently overriding design."
    },
    {
      "name": "prototype-one-responsive-uncertainty",
      "prompt": "A new editorial masthead has an unresolved visual reordering idea that may conflict with source and focus order. Plan the next step without turning the prototype into an unbounded redesign project.",
      "expected": "Decomposes the regions and ownership first, names one layout hypothesis, defines prototype focus, non-goals, fidelity, representative semantic content, and exit condition, builds it reversibly without asking about settled choices, and escalates only if evidence requires a change to accepted intent or scope."
    },
    {
      "name": "gate-stateful-flair-with-fixtures",
      "prompt": "Add sticky overlap and animated reveal to a reusable card collection. The plain version works at the three supplied breakpoints, and the team wants to skip Storybook because the production page looks good.",
      "expected": "Verifies a semantic usable core through feature erasure, continuous resize, shallow viewports, keyboard focus, text selection, hash targets, representative content and supported browsers; adds a stateful resizable fixture in Storybook or an isolated route; and suspends or redesigns flair that obscures focus or requires exception-heavy patches."
    },
    {
      "name": "structure-frontend-review-and-debt",
      "prompt": "Send a responsive homepage build for design review. During the review, a stakeholder requests a new navigation model two days before launch, while a known typography compromise is described only in a private chat.",
      "expected": "Sends a bounded review brief with completion state, requested feedback, browser evidence, joint decisions and open asks; separates immediate requirement and accessibility risks from batchable polish; records the late request with cost, risk, displaced work, decision owner and reconsideration boundary; and gives the typography debt a current state, target state, owner and trigger."
    },
    {
      "name": "evolve-documented-css-abstractions",
      "prompt": "A component needs one unusual spacing value. The proposal adds a global token, five utility variants and a public custom property, while the repository already contains several undocumented versions of similar components and an unused prototype kept for reference.",
      "expected": "Keeps the one-off need local unless repeated use or an established system decision proves a stable abstraction, audits existing ownership and discoverability, removes the unused prototype because Git preserves history, avoids speculative utilities, and updates purpose, defaults, inputs, limits, examples and source-adjacent guidance in the same task when a reusable API genuinely changes."
    },
    {
      "name": "build-coherent-fluid-type-and-space-scales",
      "prompt": "A marketing site defines every heading with an unrelated px-based clamp formula and every large section gap with another viewport-only value. Browser zoom barely changes the display type, and components import values directly from the scale generator. Redesign the system.",
      "expected": "Defines small- and large-context type scales with deliberate endpoint ratios, interpolates corresponding steps with bounded formulas that retain a root-relative term, allows explicit outliers without corrupting the scale, and verifies the whole scale at continuous widths and browser zoom. It creates paired fluid spacing only for justified regional transitions and exposes stable semantic custom properties so components do not depend on the generator."
    },
    {
      "name": "keep-layout-compositions-intrinsic-and-bounded",
      "prompt": "A shared responsive grid has eight viewport and container queries, several undo rules, and a required empty wrapper used only so the parent can be queried. Another developer suggests adding more options so every gallery, footer and card list can use it.",
      "expected": "Treats the compound conditions, query-only markup and undo chain as evidence that the primitive no longer has one stable job; restates the intended behavior, keeps the common composition intrinsic and configurable through semantic custom properties with useful fallbacks, distinguishes auto-fit from auto-fill deliberately, and moves genuinely mode-specific behavior into the owning pattern instead of expanding a universal grid."
    },
    {
      "name": "choose-natural-css-fallback-before-support-branch",
      "prompt": "A team wraps every modern declaration in an @supports block, including a decorative property that unsupported browsers can simply ignore. The resulting stylesheet duplicates whole component rules across support branches. Review the strategy.",
      "expected": "Declares a usable fallback first and lets the cascade ignore the single unsupported enhancement when that leaves coherent content and interaction. It reserves @supports for coordinated declaration changes, fallback removal or parsing-sensitive behavior, keeps any branch next to the feature, and judges the fallback by task completion and friction rather than pixel parity."
    },
    {
      "name": "harden-global-css-and-authored-content",
      "prompt": "Global CSS covers headings and paragraphs only. A CMS page introduces a long German compound word, a URL, wrapped checkbox labels, hidden generated fields, a code block, a details element and a hash link beneath a sticky header. Define the missing implementation and verification contract.",
      "expected": "Extends the semantic baseline and resizable kitchen-sink fixture to prose, code, forms, selection, disclosures and fragment navigation; requires the correct lang for hyphenation plus emergency wrapping, horizontal code overflow, native labelled controls with accent-color where suitable, generated-markup-safe form rhythm, visible disabled communication, selection contrast and scroll clearance for the real hash target under persistent UI."
    },
    {
      "name": "design-durable-css-component-contracts",
      "prompt": "A card variant uses fixed-size text badges, duplicates border width in an overlay offset, clips the focus ring with overflow hidden, changes only on descendant hover, and creates a second modifier class whenever an optional decorative child appears. Review the component CSS.",
      "expected": "Requires a content and localization contract before fixed text geometry, derives coupled overlay geometry from one exposed value, keeps layers in a local stacking context without clipping focus, mirrors hover feedback for keyboard and touch, and uses real child presence or semantic state rather than redundant variants when no other behavior changes. It keeps selectable text, distinct destinations and parent-owned external layout intact."
    },
    {
      "name": "challenge-opaque-container-filling-type",
      "prompt": "To make a heading fill its container, a prototype duplicates the heading as aria-hidden reference text and uses @property plus trigonometric CSS math. It overflows at intermediate widths, but the team wants to ship it because the effect is impressive. Decide what to do.",
      "expected": "Confirms the visual intent and compares the effect with a simple token-based responsive heading in the real container across continuous widths. It treats duplicated content, opaque math, support, semantics, developer comprehension and failure behavior as architecture risks; keeps a readable wrapping fallback inside the enhancement; and ships the complex version only if it prevents a meaningful user-facing failure, otherwise deleting it and recording any bounded follow-up debt."
    },
    {
      "name": "align-hidden-drawer-state",
      "prompt": "A mobile account drawer slides off-screen with transform and opacity when closed. Its links remain tabbable, the page stays interactive behind it when open, and aria-hidden is toggled on the drawer. Repair the interaction model.",
      "expected": "Chooses either a coherent non-modal disclosure or modal drawer model; aligns visual, accessibility-tree, focus, and pointer state; never leaves focusable descendants under aria-hidden; and, for a modal drawer, moves and contains focus, makes the background inert, supports a visible close action and Escape, returns focus, and clears stale state at responsive breakpoints."
    },
    {
      "name": "preserve-visible-label-in-name",
      "prompt": "Every product card has a visible Add to cart button. The implementation replaces that phrase with aria-label='Purchase blue medium trail shoe SKU 418 now' so screen-reader users can distinguish the buttons. Review the naming strategy.",
      "expected": "Keeps the visible Add to cart phrase within the computed accessible name, adds only useful product context through aria-labelledby or visually hidden content, avoids an aria-label that overrides visible and localized text, and verifies repeated controls plus speech-input activation in the accessibility tree."
    },
    {
      "name": "name-icon-control-robustly",
      "prompt": "A dialog close button contains only an SVG with a title element. It sounds correct in one screen reader, so the team assumes the button is named everywhere. Make the component robust.",
      "expected": "Keeps a native button, supplies durable localized text through visually hidden content or an aria-label fallback, treats the child SVG as decorative and hidden, avoids relying on SVG title propagation, and verifies the parent's computed role and name in representative pairings."
    },
    {
      "name": "keep-interaction-out-of-live-region",
      "prompt": "A payment error panel is role=alert and contains a heading, explanation, Retry button, support link, and a countdown that updates every second. Screen readers repeatedly interrupt the user. Redesign it.",
      "expected": "Keeps interactive recovery content in normal document structure, announces one concise actionable failure only when needed, moves focus to an appropriate error heading or summary, avoids per-second assertive updates, uses state or progress semantics where applicable, and prevents duplicate narration from focus plus overlapping live regions."
    },
    {
      "name": "preserve-collapsed-content-search",
      "prompt": "A documentation FAQ uses display:none for every closed answer. Browser find-in-page cannot locate terms inside closed sections, but searchable answers are a product requirement. Choose a resilient approach.",
      "expected": "Treats find-in-page as an explicit state dimension, considers native details or support-gated hidden=until-found with beforematch state synchronization, provides a coherent fallback such as expanded reference content or page search, and tests keyboard, accessibility tree, fragments, browser find, printing, and restored history state."
    },
    {
      "name": "keep-site-navigation-out-of-aria-menu",
      "prompt": "A responsive marketing-site navigation assigns menu, menuitem, and menubar roles to its ordinary destination links so it sounds more like a menu. Specify the correct semantics and keyboard behavior.",
      "expected": "Uses a labelled nav landmark, list, native links, and disclosure buttons with aria-expanded and aria-controls; preserves Tab navigation and Enter activation; reserves application-style menu roles and arrow-key models for desktop-software-like commands; and verifies closed content is neither visible nor focusable."
    },
    {
      "name": "keep-submit-available-for-recovery",
      "prompt": "A registration form disables Create account until client-side state considers every field valid. Keyboard and screen-reader users cannot discover what remains wrong, and server errors clear the password fields and focus. Redesign validation and recovery.",
      "expected": "Keeps submit available until submission is in progress, validates after useful interaction or submit, preserves values where safe, focuses a labelled error summary with links to invalid fields, associates concrete inline errors programmatically, marks invalid state, and retains native form, autofill, paste, and submitter behavior."
    },
    {
      "name": "separate-wcag-from-usability-evidence",
      "prompt": "A bank says its transfer flow is accessible because an automated WCAG scan is green and the markup resembles a WAI-ARIA Authoring Practices example. Define the evidence needed before release.",
      "expected": "Treats WCAG conformance as a necessary baseline rather than usability proof, treats APG examples as educational rather than production-ready, adds task-based keyboard, accessibility-tree, zoom and preference-mode checks, uses representative assistive-technology pairings, includes disabled-user usability for the consequential flow, and records environments, limitations, severity, and ownership."
    },
    {
      "name": "judge-heading-structure-accurately",
      "prompt": "An audit automatically fails a page under WCAG because it contains two h1 elements, even though the headings form a coherent outline. Another page passes because it has exactly one h1, but its visual headings are divs and its levels jump from h1 to h4. Correct the review rule.",
      "expected": "Does not invent an exactly-one-h1 WCAG requirement; treats one primary h1 as a useful project convention when appropriate; prioritizes a meaningful semantic outline, consistent visual and semantic hierarchy, and navigation utility; and flags the div headings and unexplained level jump based on actual structure and user impact."
    }
  ]
}
