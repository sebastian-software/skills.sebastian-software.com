interface:
  display_name: "PR Review"
  short_description: "Review PRs or classify supplied review context"
  default_prompt: "Use $pr-review to review the relevant pull requests or return a caller-owned analysis handoff, respecting the requested authority and delivery boundaries."
